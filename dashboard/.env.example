# Optional: Tickets API (Rails) – base URL and repo for stored tickets
# TICKETS_API_URL=http://localhost:3000
# REPO_IDENTIFIER=owner/repo

# Optional: Supabase for realtime updates (subscribe to tickets table changes)
# Get from Supabase: Project Settings → API → URL and anon key
# SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_ANON_KEY=eyJ...
