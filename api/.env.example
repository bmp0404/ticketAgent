# GitHub (required for API and sync)
GITHUB_ACCESS_TOKEN=ghp_xxxx

# Supabase Postgres – use Connection string → Session pooler URI (not Direct).
# Session pooler host: aws-0-<region>.pooler.supabase.com, port 5432 or 6543.
# Replace [YOUR-PASSWORD] with your Supabase database password.
DATABASE_URL=postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# Optional
PORT=3000

# Optional: URL to POST when tickets are updated (e.g. after GitHub webhook). Dashboard can use Supabase Realtime instead.
# FRONTEND_WEBHOOK_URL=https://your-dashboard.com/webhooks/tickets
